# site build output (regenerated by site/build)
_site/

# local untracked, mode-600 keys (only present in dev checkouts)
.xenv.keys

# local dev-environment files (developer-specific, never shared)
.envrc
.claude/

# rust build output (regenerated by cargo)
recipes/rust/target/
